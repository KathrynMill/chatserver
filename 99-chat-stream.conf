stream {
    upstream chatserver_cluster {
        server 127.0.0.1:6004 weight=1;
        server 127.0.0.1:6006 weight=1;
    }

    server {
        listen 8000;
        proxy_pass chatserver_cluster;
        proxy_timeout 300s;
        proxy_connect_timeout 1s;
    }
}
